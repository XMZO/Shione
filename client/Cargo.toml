[package]
name = "shione-client"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
icu_provider = { version = "2.2.0", features = ["logging"] }
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
slint = "1.16.1"

[build-dependencies]
slint-build = "1.16.1"
